{{#if page.attributes.show_logout}}
<div class="navbar-item hide-for-print">
  <span class="control">
    <a class="button is-primary" href="{{site.keys.zendeskHcUrl}}">Back to Zendesk</a>
  </span>
</div>
{{/if}}
